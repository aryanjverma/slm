__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Keep data/eval artifacts, but avoid accidentally committing large checkpoints.
artifacts/models/

# College Board PDF binaries — use catalog_ap_sources.py to download locally.
artifacts/raw/ap_central/*.pdf
